import 'package:flutter/material.dart';

class GuidesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Guides Page', style: TextStyle(color: Colors.white)),
    );
  }
}
